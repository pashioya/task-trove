/*import gql from 'graphql-tag';

const UPDATE_ITEM_MUTATION = gql`
  mutation updateItem($itemId: Int!, $boardId: Int!, $columnId: String!, $value: JSON!) {
    change_multiple_column_values(item_id: $itemId, board_id: $boardId, column_values: { $columnId: $value }) {
      id
    }
  }
`;

export default UPDATE_ITEM_MUTATION;*/
